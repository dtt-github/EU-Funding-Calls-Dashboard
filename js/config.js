/*
 * Supabase configuration.
 *
 * Replace the placeholder values below with your own Supabase project
 * credentials.  Both values are safe to expose in client-side code —
 * Row Level Security on the database protects user data.
 *
 * You can find them in your Supabase dashboard under:
 *   Settings  →  API  →  Project URL / anon public key
 */
var SUPABASE_URL  = 'https://YOUR_PROJECT.supabase.co';
var SUPABASE_ANON = 'YOUR_ANON_KEY';
